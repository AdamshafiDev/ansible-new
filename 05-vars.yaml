- name: varible
  hosts: local
  connection: local
  vars:
    Cousse: ansible
    duration: 10HR
    trainer: adam

  tasks:
      - name: couser information
        ansible.builtin.debug:
         msg: "hello {{ course }} course {{ duration }} course {{ trainer }}"